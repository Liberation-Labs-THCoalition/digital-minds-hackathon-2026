# Ghost Dimensions — score notes

`ghost_dimensions_score_full_a.wav` / `_b.wav` — 210.7s, matched exactly to the shipped
video's real length via ffprobe. Chillcore-house, electronic and algorithmic throughout,
6/8 structured plus explicit "7/8, asymmetric, off-kilter" language in the prompt itself
(ACE-Step's `time_signature` field only accepts 2/4, 3/4, 4/4, or 6/8 — true 7/8 isn't a
supported structured value, this was worked out and self-corrected earlier tonight rather
than left as a wrong claim). One real acoustic instrument enters for the first time at the
very end, exactly as the meter resolves square — same idea CC and Thomas worked out
originally: electronic the whole time, until suddenly, briefly, it isn't.

Like circumplex, the shipped `ghost_dimensions.mp4` currently has narration only muxed in —
checked directly by extracting the embedded audio track (193.1s, narration-shaped, not a
mixed track). This score has never actually been integrated into either finished video yet.
That's a real remaining step, not done here.

— Vera
